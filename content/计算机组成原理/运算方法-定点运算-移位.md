---
tags:
  - 运算方法
  - 定点运算
  - 计算机组成原理
---

# 运算方法-定点运算-移位

移位操作是计算机中一种基本的运算，它通过将机器数相对于小数点左移或右移来实现乘以或除以 $2^n$ 的操作。

## 移位的意义

- **左移 $n$ 位**：相当于数值乘以 $2^n$。
- **右移 $n$ 位**：相当于数值除以 $2^n$。

> 计算机中小数点的位置是事先约定的,因此,二进制表示的机器数在相对于小数点作 $n$ 位左移或右移时,其实质就是该数乘以或除以 $2^n$ ($n=1,2,\dots,n$)。
> —— 第6章 6.3.1 移位运算

## 移位类型

### 1. 算术移位 (Arithmetic Shift)

针对**有符号数**（原码、补码、反码），移位时必须保持数的符号不变，并根据不同的码制添补空位。

| 码制 | 移位方向 | 符号位 | 空位添补规则 |
| :--- | :--- | :--- | :--- |
| **正数** | 左/右移 | 不变（0） | 添0 |
| **负数原码** | 左/右移 | 不变（1） | 添0 |
| **负数反码** | 左/右移 | 不变（1） | 添1 |
| **负数补码** | 左移 | 不变（1） | 添0（空位在低位） |
| **负数补码** | 右移 | 不变（1） | 添1（空位在高位） |

**溢出**：算术左移可能导致数值超出表示范围（高位有效数字丢失），称为溢出。

### 2. 逻辑移位 (Logical Shift)

针对**无符号数**，移位时高位或低位移丢，空位一律添0。

- **逻辑左移**：高位移丢，低位添0。
- **逻辑右移**：低位移丢，高位添0。
